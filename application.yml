spring:
    profiles:
        active:
            - dev
---
spring:
    profiles: dev
    application:
        name: microcloud-config-client 
eureka:
  client: 
    service-url:
      defaultZone: http://admin:admin@eureka1:7001/eureka
---
spring:
    profiles: beta
    application:
        name: microcloud-config-client 
eureka:
  client: 
    service-url:
      defaultZone: http://admin:admin@eureka1:7001/eureka